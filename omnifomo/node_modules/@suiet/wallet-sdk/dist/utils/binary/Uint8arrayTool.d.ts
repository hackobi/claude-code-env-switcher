export declare class Uint8arrayTool {
    static toString(bytes: Uint8Array, encoding?: BufferEncoding): string;
    static toHex(bytes: Uint8Array): string;
    static toBase64(bytes: Uint8Array): string;
    static fromHex(hex: string): Uint8Array;
    static fromBase64(b64: string): Uint8Array;
    static from(value: string, encoding?: BufferEncoding): Uint8Array;
    static fromArrayLike(arrayLike: {
        [key: number]: number;
    }): Uint8Array;
    static ensureUint8Array(value: unknown): Uint8Array;
    static bytesEqual(a: Uint8Array, b: Uint8Array): boolean;
}
