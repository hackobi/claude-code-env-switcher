import { IAccountObjectManager } from "./interfaces";
import { SuiClient, SuiObjectData } from "@mysten/sui/client";
export declare class AccountObjectManager implements IAccountObjectManager {
    protected client: SuiClient;
    constructor(suiClient: SuiClient);
    getOwnedObjects(address: string): Promise<SuiObjectData[]>;
    getSuiClient(): SuiClient;
    setSuiClient(suiClient: SuiClient): void;
}
