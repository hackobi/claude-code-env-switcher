import * as ABI from "../../abis"
import ContractBase from "../contract-base"
import { ConnectionConfig } from "../../types/eth"

export class CollateralPoolFactory extends ContractBase<typeof ABI.CollateralPoolFactoryABI> {
  constructor(connectionConfig: ConnectionConfig, contractAddress: string) {
    super(connectionConfig, contractAddress, ABI.CollateralPoolFactoryABI)
  }
}
