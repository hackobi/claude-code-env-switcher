import { EventLog } from "web3";
interface Job {
    id: string;
    data: EventLog;
}
export declare class JobQueueInOrder {
    queue: Job[];
    isProcessing: boolean;
    uniqueJobId: boolean;
    processJob: (job: Job) => Promise<void>;
    constructor(uniqueJobId: boolean | undefined, processJob: (job: Job) => Promise<void>);
    addJob(job: Job): void;
    processQueue(): Promise<void>;
}
export {};
//# sourceMappingURL=queue.d.ts.map