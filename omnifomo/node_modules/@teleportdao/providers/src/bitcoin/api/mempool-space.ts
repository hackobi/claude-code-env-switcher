import MempoolSpaceBase from "./mempool-space-default"

class MempoolSpace extends MempoolSpaceBase {
  constructor(testnet = false) {
    const baseUrl = testnet ? "https://mempool.space/signet/api" : "https://mempool.space/api"
    super(baseUrl)
  }
}

export default MempoolSpace
