import MempoolSpace from "./mempool-space-default"

class BlockStream extends MempoolSpace {
  constructor(testnet = false) {
    const baseUrl = testnet
      ? "https://blockstream.info/testnet/api"
      : "https://blockstream.info/api"
    super(baseUrl)
  }

  async getRecommendedFeeRate(speed: "normal" | "slow" | "fast" = "normal"): Promise<number> {
    throw new Error("not supported")
  }
}

export default BlockStream
