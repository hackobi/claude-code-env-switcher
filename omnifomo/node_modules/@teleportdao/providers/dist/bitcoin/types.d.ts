export interface Vout {
    address?: string;
    script: string;
    value: number;
}
export interface Vin {
    txId: string;
    index: number;
}
export interface ConfirmedTransaction {
    txId: string;
    version: number;
    locktime: number;
    blockNumber: number;
    blockHash: string;
    vout: Vout[];
    vin: Vin[];
    blockTime?: number;
}
export type Utxo = {
    address: string;
    txId: string;
    index: number;
    value: number;
    blockNumber?: number;
};
export interface UtxoProvider {
    getUtxos: (address: string) => Promise<Utxo[]>;
    getRawTransaction?: (address: string) => Promise<string>;
    sendRawTransaction?: (txHex: string) => Promise<string>;
}
//# sourceMappingURL=types.d.ts.map