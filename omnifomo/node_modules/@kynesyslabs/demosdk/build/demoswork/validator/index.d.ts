import { DemoScript } from "../../types/demoswork";
/**
 * Validates a work script. Checks for common errors.
 *
 * @param work The work script to validate
 */
export declare function runSanityChecks(script: DemoScript): void;
