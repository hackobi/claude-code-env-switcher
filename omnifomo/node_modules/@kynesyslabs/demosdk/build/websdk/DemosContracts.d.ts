/**
 * Smart contract functionality for Demos SDK
 */
import { Demos } from './demosclass';
import { ContractInstance } from '../contracts/ContractInstance';
import { ContractABI, ContractDeployOptions, ContractCallOptions, ContractCallResult } from '../contracts/types/ContractABI';
export declare class DemosContracts {
    private demos;
    private factory;
    constructor(demos: Demos);
    /**
     * Deploy a new smart contract
     *
     * @example
     * ```typescript
     * const contract = await demos.contracts.deploy(`
     *   class MyToken extends DemosContract {
     *     constructor() {
     *       this.state.set('totalSupply', 1000000)
     *     }
     *   }
     * `)
     * ```
     */
    deploy(source: string, constructorArgs?: any[], options?: ContractDeployOptions): Promise<ContractInstance>;
    /**
     * Get an instance of an existing contract
     *
     * @example
     * ```typescript
     * const contract = await demos.contracts.at('contract_address')
     * const result = await contract.call('balanceOf', ['my_address'])
     * ```
     */
    at<T = any>(address: string, abi?: ContractABI): Promise<ContractInstance<T>>;
    /**
     * Call a contract method directly
     *
     * @example
     * ```typescript
     * const result = await demos.contracts.call(
     *   'contract_address',
     *   'transfer',
     *   ['recipient', 100]
     * )
     * ```
     */
    call<T = any>(contractAddress: string, method: string, args?: any[], options?: ContractCallOptions): Promise<ContractCallResult<T>>;
    /**
     * Create a batch operation
     *
     * @example
     * ```typescript
     * const batch = demos.contracts.batch()
     *   .deploy(tokenContract)
     *   .call(existingContract, 'initialize', [])
     *   .call(anotherContract, 'setOwner', [newOwner])
     *
     * const results = await batch.execute()
     * ```
     */
    batch(): import("../contracts/ContractFactory").BatchBuilder;
    /**
     * Estimate gas for a contract call
     *
     * @example
     * ```typescript
     * const gasEstimate = await demos.contracts.estimateGas(
     *   'contract_address',
     *   'transfer',
     *   ['recipient', 100]
     * )
     * ```
     */
    estimateGas(contractAddress: string, method: string, args?: any[]): Promise<bigint>;
    /**
     * Deploy a contract from a template
     *
     * @example
     * ```typescript
     * const token = await demos.contracts.deployTemplate('Token', {
     *   TOKEN_NAME: 'MyToken',
     *   TOKEN_SYMBOL: 'MTK',
     *   TOTAL_SUPPLY: 1000000,
     *   DECIMALS: 18
     * })
     * ```
     */
    deployTemplate(templateName: string, params?: Record<string, any>): Promise<ContractInstance>;
    /**
     * Get available contract templates
     *
     * @example
     * ```typescript
     * const templates = demos.contracts.getAvailableTemplates()
     * // ['Token', 'Storage']
     * ```
     */
    getAvailableTemplates(): string[];
    /**
     * Get template information and parameters
     *
     * @example
     * ```typescript
     * const schema = demos.contracts.getTemplateSchema('Token')
     * console.log(schema.parameters) // List of required/optional parameters
     * ```
     */
    getTemplateSchema(templateName: string): import("../contracts/templates/TemplateValidator").TemplateSchema;
    /**
     * Validate template parameters before deployment
     *
     * @example
     * ```typescript
     * const validation = demos.contracts.validateTemplate('Token', {
     *   TOKEN_NAME: 'MyToken',
     *   TOTAL_SUPPLY: 1000000
     * })
     *
     * if (!validation.valid) {
     *   console.error('Validation errors:', validation.errors)
     * }
     * ```
     */
    validateTemplate(templateName: string, params: Record<string, any>): import("../contracts/templates/TemplateValidator").ValidationResult;
    /**
     * Get usage example for a template
     *
     * @example
     * ```typescript
     * const example = demos.contracts.getTemplateExample('Token')
     * console.log(example) // Shows deployment and usage example
     * ```
     */
    getTemplateExample(templateName: string): string | null;
    /**
     * Extract constructor arguments from template parameters
     */
    private extractConstructorArgs;
}
