import { Keypair, VersionedTransaction } from "@solana/web3.js";
import { SOLANA as SolanaCore } from "../core";
import { PhantomProvider } from "../core/types/SolanaPhantomProvider";
import { SolanaSignTxOptions } from "../core/types/interfaces";
export declare function detectPhantomProvider(): PhantomProvider;
export declare class SOLANA extends SolanaCore {
    wallet: Keypair | PhantomProvider;
    constructor(rpc_url: string);
    /**
     * Connect a wallet. If running on the browser, try to connect to the Phantom wallet if no private key is provided.
     *
     * @param privateKey The private key to connect to the wallet
     * @returns The wallet object
     */
    connectWallet(privateKey?: string): Promise<Keypair | PhantomProvider>;
    signTransactions(transactions: VersionedTransaction[], options?: SolanaSignTxOptions): Promise<Buffer<ArrayBufferLike>[] | Uint8Array<ArrayBufferLike>[]>;
    disconnect(): Promise<boolean>;
}
