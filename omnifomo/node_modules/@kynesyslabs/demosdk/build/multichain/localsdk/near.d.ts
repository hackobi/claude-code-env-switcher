import { KeyPair } from "near-api-js";
import { IDefaultChainLocal, NEAR as NearCore, TransactionResponse } from "../core";
export declare class NEAR extends NearCore implements IDefaultChainLocal {
    sendTransaction(signed_tx: Uint8Array): Promise<TransactionResponse>;
    getInfo(): Promise<void>;
    createWallet(): Promise<KeyPair>;
}
