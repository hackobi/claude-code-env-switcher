"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.BTC = void 0;
const btc_1 = require("../core/btc");
const utils_1 = require("../core/utils");
const axios_1 = __importDefault(require("axios"));
const bitcoin = __importStar(require("bitcoinjs-lib"));
const interfaces_1 = require("../core/types/interfaces");
class BTC extends btc_1.BTC {
    constructor(rpcUrl, network = bitcoin.networks.testnet) {
        super(rpcUrl, network);
    }
    async sendTransaction(signedTxHex) {
        (0, utils_1.required)(this.provider, "Provider not connected");
        try {
            const url = this.provider.includes("blockstream.info") &&
                this.provider.endsWith("/api")
                ? `${this.provider.slice(0, -4)}/api/tx`
                : `${this.provider}/tx`;
            const response = await axios_1.default.post(url, signedTxHex, {
                headers: { "Content-Type": "text/plain" },
            });
            // Blockstream returns txid as a string; adjust if other providers differ
            if (response.data) {
                return {
                    result: interfaces_1.XmTransactionResult.success,
                    hash: response.data,
                };
            }
            else {
                throw new Error("Failed to send transaction: no txid in response");
            }
        }
        catch (error) {
            console.error("Error sending transaction:", error);
            throw new Error(`Failed to send transaction: ${axios_1.default.isAxiosError(error) ? error.message : String(error)}`);
        }
    }
    static createInstance(networkType, rpcUrl, network) {
        if (!BTC.instances.has(networkType)) {
            const instance = new BTC(rpcUrl, network);
            BTC.instances.set(networkType, instance);
        }
        const instance = BTC.instances.get(networkType);
        if (instance) {
            return instance;
        }
        throw new Error("Could not create or retrieve instance");
    }
}
exports.BTC = BTC;
BTC.instances = new Map();
//# sourceMappingURL=btc.js.map