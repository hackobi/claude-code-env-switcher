import { IDefaultChainLocal, SOLANA as SolanaCore, TransactionResponse } from "../core";
export declare class SOLANA extends SolanaCore implements IDefaultChainLocal {
    constructor(rpc_url: string);
    sendTransaction(serialized_tx: Uint8Array): Promise<TransactionResponse>;
    getInfo(): Promise<void>;
}
