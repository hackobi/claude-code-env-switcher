import { BTC as BTCCore } from "../core/btc";
import * as bitcoin from "bitcoinjs-lib";
export declare class BTC extends BTCCore {
    private static instances;
    constructor(rpcUrl: string, network?: bitcoin.Network);
    sendTransaction(signedTxHex: string): Promise<{
        result: string;
        hash: string;
    }>;
    static createInstance(networkType: string, rpcUrl: string, network: bitcoin.Network): BTC;
}
