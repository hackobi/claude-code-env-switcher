import { IDefaultChainLocal } from "../core";
import { TON as TONCore } from "../core/ton";
import { XmTransactionResult } from "../core/types/interfaces";
export declare class TON extends TONCore implements IDefaultChainLocal {
    getInfo(): Promise<void>;
    createWallet(password?: string): Promise<string[]>;
    sendTransaction(signedTx: Buffer): Promise<{
        hash: string;
        result: XmTransactionResult;
        error?: undefined;
    } | {
        error: unknown;
        result: XmTransactionResult;
        hash?: undefined;
    }>;
}
