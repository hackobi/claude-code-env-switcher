export interface AddressInfo {
    pubkey: string;
    assignedTxs: string[];
    identities: {
        xm: Map<string, string[]>;
        web2: Map<string, string[]>;
    };
    nonce: number;
    balance: bigint;
}
