import * as forge from "node-forge";
import * as websdk from "../websdk";
import { Address } from "../types/blockchain/WalletTypes";
import { RPCResponseWithValidityData } from "../types/communication/rpc";
export default class Wallet {
    private static instances;
    ed25519: forge.pki.KeyPair;
    ed25519_hex: {
        privateKey: string;
        publicKey: string;
    };
    rsa: forge.pki.rsa.KeyPair;
    rsa_hex: {
        privateKey: string;
        publicKey: string;
    };
    private constructor();
    static getInstance(name: string): Wallet;
    create(): Promise<void>;
    loadFromKey(privateKey: Address): Promise<void>;
    load(filename: string): Promise<void>;
    save(filename: string): Promise<void>;
    getBalance(): Promise<void>;
    transfer(to: Address, amount: number, demos: websdk.Demos): Promise<RPCResponseWithValidityData>;
    broadcast(validityData: RPCResponseWithValidityData, demos: websdk.Demos): Promise<any>;
    generatePasskey(): Promise<string>;
}
