"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.quoteExactOutput = exports.quoteExactInput = exports.getUniversalQuoterContract = void 0;
const utils_1 = require("../../base/utils");
const abi_json_1 = __importDefault(require("./abi.json"));
const utils_2 = require("../utils");
const base_1 = require("../../base");
const getUniversalQuoterContract = (address, web3) => {
    return (0, utils_1.getEVMContract)(abi_json_1.default, address, web3);
};
exports.getUniversalQuoterContract = getUniversalQuoterContract;
function convertQuoterPriceListToPriceList(tokenChain, quoterPriceList) {
    const priceList = quoterPriceList.map((p, i) => {
        const usePoint = Boolean(p.usePoint);
        if (usePoint) {
            return {
                isV2: false,
                point: Number(p.point)
            };
        }
        else {
            const reserveIn = BigInt(p.reserveIn).toString();
            const reserveOut = BigInt(p.reserveOut).toString();
            const tokenInAddress = (0, base_1.getSwapTokenAddress)(tokenChain[i]).toLowerCase();
            const tokenOutAddress = (0, base_1.getSwapTokenAddress)(tokenChain[i + 1]).toLowerCase();
            const tokenInIsToken0 = (tokenInAddress < tokenOutAddress);
            return {
                isV2: true,
                reserve0: tokenInIsToken0 ? reserveIn : reserveOut,
                reserve1: tokenInIsToken0 ? reserveOut : reserveIn,
                reserveIn,
                reserveOut,
            };
        }
    });
    return priceList;
}
const quoteExactInput = (universalQuoter, params, limit) => __awaiter(void 0, void 0, void 0, function* () {
    const path = (0, utils_2.getUniversalHexPath)(params.feeTier.slice(), params.isV2.slice(), params.tokenChain.slice());
    const quoterParams = {
        amount: params.inputAmount,
        path,
        limit,
        outFee: (0, utils_2.outFeeTier2OutFeeContractNumber)(params.outChargeFeeTier)
    };
    const { acquire, price } = yield universalQuoter.methods.swapAmount(quoterParams).call();
    return {
        outputAmount: acquire.toString(),
        poolPriceList: convertQuoterPriceListToPriceList(params.tokenChain, price.slice())
    };
});
exports.quoteExactInput = quoteExactInput;
const quoteExactOutput = (universalQuoter, params, limit) => __awaiter(void 0, void 0, void 0, function* () {
    const path = (0, utils_2.getUniversalHexPath)(params.feeTier.slice().reverse(), params.isV2.slice().reverse(), params.tokenChain.slice().reverse());
    const quoterParams = {
        amount: params.outputAmount,
        path,
        limit,
        outFee: (0, utils_2.outFeeTier2OutFeeContractNumber)(params.outChargeFeeTier)
    };
    const { cost, price: rawPriceList } = yield universalQuoter.methods.swapDesire(quoterParams).call();
    const price = rawPriceList.slice().reverse();
    return {
        inputAmount: cost.toString(),
        poolPriceList: convertQuoterPriceListToPriceList(params.tokenChain, price)
    };
});
exports.quoteExactOutput = quoteExactOutput;
