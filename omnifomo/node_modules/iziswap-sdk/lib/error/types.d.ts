export declare enum SDKModule {
    SWAP_QUERY = "SWAP_QUERY",
    POOL = "POOL"
}
export declare class iZiSwapError extends Error {
    code: string;
    module: SDKModule;
    constructor(module: SDKModule, code: string, msg?: string);
}
