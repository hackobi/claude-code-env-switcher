export declare namespace BinarySearch {
    enum FindLeftOperator {
        LESS_THAN = 0,
        LESS_THAN_OR_EQUAL = 1
    }
    enum FindRightOperator {
        GREATER_THAN = 0,
        GREATER_THAN_OR_EQUAL = 1
    }
    function isLeft(target: number, input: number, opt: FindLeftOperator): boolean;
    function isRight(target: number, input: number, opt: FindRightOperator): boolean;
    function findLeft(array: number[], input: number, opt: FindLeftOperator): number;
    function findRight(array: number[], input: number, opt: FindRightOperator): number;
}
