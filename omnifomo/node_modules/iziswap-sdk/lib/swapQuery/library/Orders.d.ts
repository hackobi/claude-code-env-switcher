import JSBI from "jsbi";
export declare namespace Orders {
    interface Orders {
        liquidity: JSBI[];
        liquidityDeltaPoint: number[];
        sellingX: JSBI[];
        sellingXPoint: number[];
        sellingY: JSBI[];
        sellingYPoint: number[];
    }
    interface Cursor {
        liquidityIdx: number;
        sellingIdx: number;
        currentPoint: number;
        isLiquidityPoint: boolean;
        isLimitOrderPoint: boolean;
    }
    function findLeftCursor(orders: Orders, currentPoint: number): Cursor;
    function findRightCursor(orders: Orders, currentPoint: number): Cursor;
    function findLeftFromCursor(orders: Orders, currentCursor: Cursor, currentPoint: number): Cursor;
    function findRightFromCursor(orders: Orders, currentCursor: Cursor, currentPoint: number): Cursor;
    function coverLowPoint(orders: Orders, lowPt: number): boolean;
    function coverHighPoint(orders: Orders, highPt: number): boolean;
    function coverCurrentPoint(orders: Orders, currentPt: number): boolean;
    function nearestLeftOneOrBoundary(orders: Orders, currentCursor: Cursor, point: number, pointDelta: number): number;
    function nearestRightOneOrBoundary(orders: Orders, currentCursor: Cursor, point: number, pointDelta: number): number;
}
