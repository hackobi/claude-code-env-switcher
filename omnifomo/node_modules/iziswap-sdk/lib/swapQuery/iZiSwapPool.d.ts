import JSBI from "jsbi";
import { Orders } from "./library/Orders";
import { SwapQuery } from "./library/State";
export declare class iZiSwapPool {
    private readonly _state;
    readonly orders: Orders.Orders;
    readonly leftMostPt: number;
    readonly rightMostPt: number;
    readonly sqrtRate_96: JSBI;
    readonly pointDelta: number;
    readonly fee: number;
    constructor(state: SwapQuery.State, orders: Orders.Orders, sqrtRate_96: JSBI, pointDelta: number, fee: number);
    get state(): SwapQuery.State;
}
