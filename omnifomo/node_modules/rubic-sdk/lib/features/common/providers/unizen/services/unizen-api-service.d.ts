import { TxStatusData } from "../../../status-manager/models/tx-status-data";
import { UniZenQuoteParams } from '../models/unizen-quote-params';
import { UniZenSwapParams } from '../models/unizen-swap-params';
export declare class UniZenApiService {
    private static apiEndpoint;
    static getQuoteInfo<T>(params: UniZenQuoteParams, chainId: number, tradeType: 'cross' | 'single'): Promise<T>;
    static getSwapInfo<T>(params: UniZenSwapParams, chainId: number, tradeType: 'cross' | 'single'): Promise<T>;
    static getTxStatus(srcTxHash: string): Promise<TxStatusData>;
}
