"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.UniZenCcrTrade = void 0;
const bignumber_js_1 = __importDefault(require("bignumber.js"));
const blockchain_id_1 = require("../../../../../core/blockchain/utils/blockchains-info/constants/blockchain-id");
const evm_web3_pure_1 = require("../../../../../core/blockchain/web3-pure/typed-web3-pure/evm-web3-pure/evm-web3-pure");
const unizen_api_service_1 = require("../../../../common/providers/unizen/services/unizen-api-service");
const get_from_without_fee_1 = require("../../../../common/utils/get-from-without-fee");
const cross_chain_trade_type_1 = require("../../models/cross-chain-trade-type");
const rubic_proxy_contract_address_1 = require("../common/constants/rubic-proxy-contract-address");
const evm_common_cross_chain_abi_1 = require("../common/evm-cross-chain-trade/constants/evm-common-cross-chain-abi");
const gateway_rubic_cross_chain_abi_1 = require("../common/evm-cross-chain-trade/constants/gateway-rubic-cross-chain-abi");
const evm_cross_chain_trade_1 = require("../common/evm-cross-chain-trade/evm-cross-chain-trade");
const bridge_type_1 = require("../common/models/bridge-type");
const proxy_cross_chain_evm_trade_1 = require("../common/proxy-cross-chain-evm-facade/proxy-cross-chain-evm-trade");
const unizen_ccr_utils_service_1 = require("./services/unizen-ccr-utils-service");
class UniZenCcrTrade extends evm_cross_chain_trade_1.EvmCrossChainTrade {
    get methodName() {
        return 'startBridgeTokensViaGenericCrossChain';
    }
    constructor(ccrTrade, providerAddress, routePath, useProxy) {
        super(providerAddress, routePath, useProxy);
        this.type = cross_chain_trade_type_1.CROSS_CHAIN_TRADE_TYPE.UNIZEN;
        this.isAggregator = false;
        this.onChainSubtype = { from: undefined, to: undefined };
        this.bridgeType = bridge_type_1.BRIDGE_TYPE.UNIZEN;
        this.from = ccrTrade.from;
        this.to = ccrTrade.to;
        this.feeInfo = ccrTrade.feeInfo;
        this.gasData = ccrTrade.gasData;
        this.slippageTolerance = ccrTrade.slippage;
        this.toTokenAmountMin = ccrTrade.toTokenAmountMin;
        this.priceImpact = ccrTrade.priceImpact;
        this.contractAddress = ccrTrade.contractAddress;
    }
    get fromBlockchain() {
        return this.from.blockchain;
    }
    get fromContractAddress() {
        return this.isProxyTrade
            ? rubic_proxy_contract_address_1.rubicProxyContractAddress[this.fromBlockchain].gateway
            : this.contractAddress;
    }
    async getContractParams(options) {
        const { data, value: providerValue, to } = await this.setTransactionConfig(false, options?.useCacheData || false, options?.receiverAddress);
        try {
            const bridgeData = proxy_cross_chain_evm_trade_1.ProxyCrossChainEvmTrade.getBridgeData(options, {
                walletAddress: this.walletAddress,
                fromTokenAmount: this.from,
                toTokenAmount: this.to,
                toAddress: undefined,
                srcChainTrade: null,
                providerAddress: this.providerAddress,
                type: `native:${this.type}`,
                fromAddress: this.walletAddress
            });
            const fromWithoutFee = (0, get_from_without_fee_1.getFromWithoutFee)(this.from, this.feeInfo.rubicProxy?.platformFee?.percent);
            const extraNativeFee = this.from.isNative
                ? new bignumber_js_1.default(providerValue).minus(fromWithoutFee.stringWeiAmount).toFixed()
                : new bignumber_js_1.default(providerValue).toFixed();
            const providerData = await proxy_cross_chain_evm_trade_1.ProxyCrossChainEvmTrade.getGenericProviderData(to, data, this.fromBlockchain, to, extraNativeFee);
            const methodArguments = [bridgeData, providerData];
            const value = this.getSwapValue(providerValue);
            const transactionConfiguration = evm_web3_pure_1.EvmWeb3Pure.encodeMethodCall(rubic_proxy_contract_address_1.rubicProxyContractAddress[this.from.blockchain].router, evm_common_cross_chain_abi_1.evmCommonCrossChainAbi, this.methodName, methodArguments, value);
            const sendingToken = this.from.isNative ? [] : [this.from.address];
            const sendingAmount = this.from.isNative ? [] : [this.from.stringWeiAmount];
            return {
                contractAddress: rubic_proxy_contract_address_1.rubicProxyContractAddress[this.from.blockchain].gateway,
                contractAbi: gateway_rubic_cross_chain_abi_1.gatewayRubicCrossChainAbi,
                methodName: 'startViaRubic',
                methodArguments: [sendingToken, sendingAmount, transactionConfiguration.data],
                value
            };
        }
        catch (err) {
            console.log(err?.message);
            throw err;
        }
    }
    async getTransactionConfigAndAmount(receiverAddress) {
        const quoteInfo = await this.getQuoteInfo(receiverAddress);
        const swapParams = {
            transactionData: quoteInfo.transactionData,
            nativeValue: quoteInfo.nativeValue,
            account: this.walletAddress,
            receiver: receiverAddress || this.walletAddress
        };
        const tradeType = 'cross';
        const swapInfo = await unizen_api_service_1.UniZenApiService.getSwapInfo(swapParams, quoteInfo.sourceChainId, tradeType);
        const evmConfig = {
            data: swapInfo.data,
            to: this.contractAddress,
            value: swapInfo.nativeValue
        };
        const toAmount = quoteInfo.transactionData.params.actualQuote;
        return { config: evmConfig, amount: toAmount };
    }
    async getQuoteInfo(receiverAddress) {
        const fromWithoutFee = (0, get_from_without_fee_1.getFromWithoutFee)(this.from, this.feeInfo.rubicProxy?.platformFee?.percent);
        const dstChainId = blockchain_id_1.blockchainId[this.to.blockchain];
        const srcChainId = blockchain_id_1.blockchainId[this.from.blockchain];
        const quoteSendParams = {
            fromTokenAddress: this.from.address,
            toTokenAddress: this.to.address,
            sender: this.walletAddress,
            slippage: this.slippageTolerance,
            amount: fromWithoutFee.stringWeiAmount,
            destinationChainId: dstChainId,
            receiver: receiverAddress
        };
        return unizen_ccr_utils_service_1.UniZenCcrUtilsService.getBestQuote(quoteSendParams, srcChainId);
    }
    getTradeInfo() {
        return {
            estimatedGas: this.estimatedGas,
            feeInfo: this.feeInfo,
            priceImpact: this.priceImpact,
            slippage: this.slippageTolerance * 100,
            routePath: this.routePath
        };
    }
}
exports.UniZenCcrTrade = UniZenCcrTrade;
//# sourceMappingURL=unizen-ccr-trade.js.map