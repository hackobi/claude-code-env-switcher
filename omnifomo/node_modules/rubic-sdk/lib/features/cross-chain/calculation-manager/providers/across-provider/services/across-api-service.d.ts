import { TxStatusData } from "../../../../../common/status-manager/models/tx-status-data";
import { AcrossFeeQuoteRequestParams, AcrossFeeQuoteResponse } from '../models/across-fee-quote';
export declare class AcrossApiService {
    private static endpoint;
    static getFeeQuote(params: AcrossFeeQuoteRequestParams): Promise<AcrossFeeQuoteResponse>;
    static getTxStatus(originChainId: number, depositId: number): Promise<TxStatusData>;
}
