"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.EddyBridgeTrade = void 0;
const bignumber_js_1 = __importDefault(require("bignumber.js"));
const errors_1 = require("../../../../../common/errors");
const evm_web3_pure_1 = require("../../../../../core/blockchain/web3-pure/typed-web3-pure/evm-web3-pure/evm-web3-pure");
const web3_pure_1 = require("../../../../../core/blockchain/web3-pure/web3-pure");
const fake_wallet_address_1 = require("../../../../common/constants/fake-wallet-address");
const calculate_rates_1 = require("./utils/calculate-rates");
const eddy_swap_controller_factory_1 = require("./utils/eddy-swap-controller-factory");
const cross_chain_trade_type_1 = require("../../models/cross-chain-trade-type");
const rubic_proxy_contract_address_1 = require("../common/constants/rubic-proxy-contract-address");
const evm_common_cross_chain_abi_1 = require("../common/evm-cross-chain-trade/constants/evm-common-cross-chain-abi");
const gateway_rubic_cross_chain_abi_1 = require("../common/evm-cross-chain-trade/constants/gateway-rubic-cross-chain-abi");
const evm_cross_chain_trade_1 = require("../common/evm-cross-chain-trade/evm-cross-chain-trade");
const bridge_type_1 = require("../common/models/bridge-type");
const proxy_cross_chain_evm_trade_1 = require("../common/proxy-cross-chain-evm-facade/proxy-cross-chain-evm-trade");
const eddy_bridge_contract_addresses_1 = require("./constants/eddy-bridge-contract-addresses");
const eddy_bridge_routing_directions_1 = require("./utils/eddy-bridge-routing-directions");
class EddyBridgeTrade extends evm_cross_chain_trade_1.EvmCrossChainTrade {
    get fromBlockchain() {
        return this.from.blockchain;
    }
    get fromContractAddress() {
        if (this.isProxyTrade)
            return rubic_proxy_contract_address_1.rubicProxyContractAddress[this.fromBlockchain].gateway;
        switch (this.routingDirection) {
            case eddy_bridge_routing_directions_1.ERD.ANY_CHAIN_TOKEN_TO_ANY_CHAIN_TOKEN:
            case eddy_bridge_routing_directions_1.ERD.ANY_CHAIN_TOKEN_TO_ZETA_TOKEN:
                return eddy_bridge_contract_addresses_1.CUSTODY_ADDRESSES[this.fromBlockchain];
            case eddy_bridge_routing_directions_1.ERD.ZETA_TOKEN_TO_ANY_CHAIN_ALL:
                return eddy_bridge_contract_addresses_1.EDDY_OMNI_CONTRACT_IN_ZETACHAIN;
            default:
                return eddy_bridge_contract_addresses_1.EDDY_OMNI_CONTRACT_IN_ZETACHAIN;
        }
    }
    get methodName() {
        return 'startBridgeTokensViaGenericCrossChain';
    }
    constructor(params) {
        super(params.providerAddress, params.routePath, params.useProxy);
        /**ABSTRACT PROPS */
        this.type = cross_chain_trade_type_1.CROSS_CHAIN_TRADE_TYPE.EDDY_BRIDGE;
        this.isAggregator = false;
        this.onChainSubtype = { from: undefined, to: undefined };
        this.bridgeType = bridge_type_1.BRIDGE_TYPE.EDDY_BRIDGE;
        this.to = params.crossChainTrade.to;
        this.from = params.crossChainTrade.from;
        this.feeInfo = params.crossChainTrade.feeInfo;
        this.gasData = params.crossChainTrade.gasData;
        this.priceImpact = params.crossChainTrade.priceImpact;
        this.slippage = params.crossChainTrade.slippage;
        this.toTokenAmountMin = web3_pure_1.Web3Pure.fromWei(this.to.weiAmountMinusSlippage(this.slippage), this.to.decimals);
        this.routingDirection = params.crossChainTrade.routingDirection;
        this.quoteOptions = params.crossChainTrade.quoteOptions;
    }
    async getContractParams(options) {
        const receiverAddress = options?.receiverAddress || this.walletAddress;
        const { data, value: providerValue, to: providerRouter } = await this.setTransactionConfig(false, options?.useCacheData || false, options?.receiverAddress || this.walletAddress);
        const bridgeData = proxy_cross_chain_evm_trade_1.ProxyCrossChainEvmTrade.getBridgeData(options, {
            walletAddress: receiverAddress,
            fromTokenAmount: this.from,
            toTokenAmount: this.to,
            srcChainTrade: null,
            providerAddress: this.providerAddress,
            type: `native:${this.bridgeType}`,
            fromAddress: this.walletAddress
        });
        const providerData = await proxy_cross_chain_evm_trade_1.ProxyCrossChainEvmTrade.getGenericProviderData(providerRouter, data, this.from.blockchain, providerRouter, '0');
        const methodArguments = [bridgeData, providerData];
        const value = this.getSwapValue(providerValue);
        const transactionConfiguration = evm_web3_pure_1.EvmWeb3Pure.encodeMethodCall(rubic_proxy_contract_address_1.rubicProxyContractAddress[this.from.blockchain].router, evm_common_cross_chain_abi_1.evmCommonCrossChainAbi, this.methodName, methodArguments, value);
        const sendingToken = this.from.isNative ? [] : [this.from.address];
        const sendingAmount = this.from.isNative ? [] : [this.from.stringWeiAmount];
        return {
            contractAddress: rubic_proxy_contract_address_1.rubicProxyContractAddress[this.from.blockchain].gateway,
            contractAbi: gateway_rubic_cross_chain_abi_1.gatewayRubicCrossChainAbi,
            methodName: 'startViaRubic',
            methodArguments: [sendingToken, sendingAmount, transactionConfiguration.data],
            value
        };
    }
    async getTransactionConfigAndAmount(_receiverAddress) {
        const evmConfig = eddy_swap_controller_factory_1.EddySwapControllerFactory.createController(this.from, this.to, this.walletAddress || fake_wallet_address_1.FAKE_WALLET_ADDRESS, this.routingDirection).getEvmConfig();
        const outputAmount = await (0, calculate_rates_1.calculateRates)(this.from, this.to, this.slippage);
        return {
            config: evmConfig,
            amount: outputAmount
        };
    }
    getTradeInfo() {
        return {
            estimatedGas: this.estimatedGas,
            feeInfo: this.feeInfo,
            priceImpact: this.priceImpact ?? null,
            slippage: this.slippage * 100,
            routePath: this.routePath
        };
    }
    async needApprove() {
        this.checkWalletConnected();
        if (this.from.isNative)
            return false;
        const allowance = await this.fromWeb3Public.getAllowance(this.from.address, this.walletAddress, this.fromContractAddress);
        // need allowance = amount + 1 wei at least
        return this.from.weiAmount.gte(allowance);
    }
    async approve(options, checkNeedApprove = true, amount = new bignumber_js_1.default(0)) {
        if (checkNeedApprove) {
            const needApprove = await this.needApprove();
            if (!needApprove) {
                throw new errors_1.UnnecessaryApproveError();
            }
        }
        this.checkWalletConnected();
        await this.checkBlockchainCorrect();
        // because of error on EddyBridge contract(they check on allowance > amount instead of allowance >= amount)
        const approveAmount = amount.plus(1);
        return this.web3Private.approveTokens(this.from.address, this.fromContractAddress, approveAmount, options);
    }
}
exports.EddyBridgeTrade = EddyBridgeTrade;
//# sourceMappingURL=eddy-bridge-trade.js.map