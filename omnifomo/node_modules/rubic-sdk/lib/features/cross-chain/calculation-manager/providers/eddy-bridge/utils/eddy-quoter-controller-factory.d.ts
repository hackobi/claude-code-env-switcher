import BigNumber from 'bignumber.js';
import { PriceToken, PriceTokenAmount } from "../../../../../../common/tokens";
import { EvmBlockchainName } from "../../../../../../core/blockchain/models/blockchain-name";
import { EddyQuoteController } from "../models/eddy-quote-controller";
export declare class EddyQuoterControllerFactory {
    private static readonly calculators;
    private constructor();
    static createController(from: PriceTokenAmount<EvmBlockchainName>, toToken: PriceToken<EvmBlockchainName>, ratioToAmount: number, gasFeeInSrcTokenUnits: BigNumber, slippageTolerance: number): EddyQuoteController;
    private static calculateDirectBridge;
    private static calculateSwapFromZetachain;
    private static calculateSwapToZetachain;
    private static calculateSwapBetweenOtherChains;
    private static calculateOnChainToAmount;
}
