"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ArchonBridgeTrade = void 0;
const bignumber_js_1 = __importDefault(require("bignumber.js"));
const native_tokens_1 = require("../../../../../common/tokens/constants/native-tokens");
const blockchain_name_1 = require("../../../../../core/blockchain/models/blockchain-name");
const evm_web3_pure_1 = require("../../../../../core/blockchain/web3-pure/typed-web3-pure/evm-web3-pure/evm-web3-pure");
const web3_pure_1 = require("../../../../../core/blockchain/web3-pure/web3-pure");
const get_from_without_fee_1 = require("../../../../common/utils/get-from-without-fee");
const cross_chain_trade_type_1 = require("../../models/cross-chain-trade-type");
const archon_contract_service_1 = require("./archon-contract-service");
const archon_bridge_abi_1 = require("./constants/archon-bridge-abi");
const archon_wrap_bridge_abi_1 = require("./constants/archon-wrap-bridge-abi");
const layer_zero_ids_1 = require("./constants/layer-zero-ids");
const rubic_proxy_contract_address_1 = require("../common/constants/rubic-proxy-contract-address");
const evm_common_cross_chain_abi_1 = require("../common/evm-cross-chain-trade/constants/evm-common-cross-chain-abi");
const gateway_rubic_cross_chain_abi_1 = require("../common/evm-cross-chain-trade/constants/gateway-rubic-cross-chain-abi");
const evm_cross_chain_trade_1 = require("../common/evm-cross-chain-trade/evm-cross-chain-trade");
const bridge_type_1 = require("../common/models/bridge-type");
const proxy_cross_chain_evm_trade_1 = require("../common/proxy-cross-chain-evm-facade/proxy-cross-chain-evm-trade");
class ArchonBridgeTrade extends evm_cross_chain_trade_1.EvmCrossChainTrade {
    get fromBlockchain() {
        return this.from.blockchain;
    }
    get fromContractAddress() {
        if (this.isProxyTrade) {
            return rubic_proxy_contract_address_1.rubicProxyContractAddress[this.fromBlockchain].gateway;
        }
        return archon_contract_service_1.ArchonContractService.getWeb3AndAddress(this.from, this.to).contract.address;
    }
    get methodName() {
        return this.onChainTrade
            ? 'swapAndStartBridgeTokensViaGenericCrossChain'
            : 'startBridgeTokensViaGenericCrossChain';
    }
    constructor(crossChainTrade, providerAddress, routePath, useProxy) {
        super(providerAddress, routePath, useProxy);
        this.onChainSubtype = { from: undefined, to: undefined };
        this.type = cross_chain_trade_type_1.CROSS_CHAIN_TRADE_TYPE.ARCHON_BRIDGE;
        this.isAggregator = false;
        this.bridgeType = bridge_type_1.BRIDGE_TYPE.ARCHON_BRIDGE;
        this.feeInfo = {};
        this.onChainTrade = null;
        this.from = crossChainTrade.from;
        this.to = crossChainTrade.to;
        this.gasData = crossChainTrade.gasData;
        this.toTokenAmountMin = crossChainTrade.to.tokenAmount;
        this.feeInfo = crossChainTrade.feeInfo;
    }
    async swapDirect(options = {}) {
        await this.checkTradeErrors();
        await this.checkAllowanceAndApprove(options);
        const { onConfirm, gasPriceOptions } = options;
        let transactionHash;
        const onTransactionHash = (hash) => {
            if (onConfirm) {
                onConfirm(hash);
            }
            transactionHash = hash;
        };
        // eslint-disable-next-line no-useless-catch
        try {
            const { data, value, to } = await this.setTransactionConfig(false, options?.useCacheData || false, options?.receiverAddress);
            await this.web3Private.trySendTransaction(to, {
                data,
                value,
                onTransactionHash,
                gasPriceOptions
            });
            return transactionHash;
        }
        catch (err) {
            throw err;
        }
    }
    async getContractParams(options) {
        const { data, value: providerValue, to: providerRouter } = await this.setTransactionConfig(false, options?.useCacheData || false, options?.receiverAddress);
        const bridgeData = proxy_cross_chain_evm_trade_1.ProxyCrossChainEvmTrade.getBridgeData(options, {
            walletAddress: this.walletAddress,
            fromTokenAmount: this.from,
            toTokenAmount: this.to,
            srcChainTrade: null,
            providerAddress: this.providerAddress,
            type: `native:${this.bridgeType}`,
            fromAddress: this.walletAddress
        });
        const extraNativeFee = this.from.isNative
            ? new bignumber_js_1.default(providerValue).minus(this.from.stringWeiAmount).toFixed()
            : new bignumber_js_1.default(providerValue).toFixed();
        const providerData = await proxy_cross_chain_evm_trade_1.ProxyCrossChainEvmTrade.getGenericProviderData(providerRouter, data, this.fromBlockchain, providerRouter, extraNativeFee);
        const methodArguments = [bridgeData, providerData];
        const value = this.getSwapValue(providerValue);
        const transactionConfiguration = evm_web3_pure_1.EvmWeb3Pure.encodeMethodCall(rubic_proxy_contract_address_1.rubicProxyContractAddress[this.from.blockchain].router, evm_common_cross_chain_abi_1.evmCommonCrossChainAbi, this.methodName, methodArguments, value);
        const sendingToken = this.from.isNative ? [] : [this.from.address];
        const sendingAmount = this.from.isNative ? [] : [this.from.stringWeiAmount];
        return {
            contractAddress: rubic_proxy_contract_address_1.rubicProxyContractAddress[this.from.blockchain].gateway,
            contractAbi: gateway_rubic_cross_chain_abi_1.gatewayRubicCrossChainAbi,
            methodName: 'startViaRubic',
            methodArguments: [sendingToken, sendingAmount, transactionConfiguration.data],
            value
        };
    }
    getTradeAmountRatio(fromUsd) {
        return fromUsd.dividedBy(this.to.tokenAmount);
    }
    getTradeInfo() {
        return {
            estimatedGas: this.estimatedGas,
            feeInfo: this.feeInfo,
            priceImpact: 0,
            slippage: 0,
            routePath: this.routePath
        };
    }
    async getTransactionConfigAndAmount(receiverAddress) {
        const { contract } = archon_contract_service_1.ArchonContractService.getWeb3AndAddress(this.from, this.to);
        const providerFee = this.feeInfo.provider?.cryptoFee?.amount || new bignumber_js_1.default(0);
        const nativeToken = native_tokens_1.nativeTokensList[this.fromBlockchain];
        const fromWithoutFee = (0, get_from_without_fee_1.getFromWithoutFee)(this.from, this.feeInfo.rubicProxy?.platformFee?.percent);
        const fromValueAmount = this.from.isNative ? fromWithoutFee.tokenAmount : new bignumber_js_1.default(0);
        const value = web3_pure_1.Web3Pure.toWei(fromValueAmount.plus(providerFee), nativeToken.decimals);
        const methodArguments = [];
        let config = null;
        if (contract.type === 'originRouter') {
            if (!this.from.isNative) {
                methodArguments.push(this.from.address);
            }
            methodArguments.push(fromWithoutFee.stringWeiAmount, receiverAddress || this.walletAddress, [this.walletAddress, '0x0000000000000000000000000000000000000000'], '0x');
            config = evm_web3_pure_1.EvmWeb3Pure.encodeMethodCall(contract.address, archon_bridge_abi_1.archonBridgeAbi, this.from.isNative ? 'bridgeNative' : 'bridge', methodArguments, value);
        }
        else {
            methodArguments.push(this.from.address, layer_zero_ids_1.layerZeroIds[this.to.blockchain], fromWithoutFee.stringWeiAmount, receiverAddress || this.walletAddress, true, [this.walletAddress, '0x0000000000000000000000000000000000000000'], '0x');
            config = evm_web3_pure_1.EvmWeb3Pure.encodeMethodCall(contract.address, archon_wrap_bridge_abi_1.archonWrapBridgeAbi, this.from.blockchain === blockchain_name_1.BLOCKCHAIN_NAME.HORIZEN_EON && this.from.isNative
                ? 'bridgeNative'
                : 'bridge', methodArguments, value);
        }
        return { config, amount: this.to.stringWeiAmount };
    }
}
exports.ArchonBridgeTrade = ArchonBridgeTrade;
//# sourceMappingURL=archon-bridge-trade.js.map