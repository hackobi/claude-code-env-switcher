"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TonBridgersCrossChainTrade = void 0;
const errors_1 = require("../../../../../../common/errors");
const not_supported_region_1 = require("../../../../../../common/errors/swap/not-supported-region");
const blockchain_name_1 = require("../../../../../../core/blockchain/models/blockchain-name");
const injector_1 = require("../../../../../../core/injector/injector");
const bridgers_native_address_1 = require("../../../../../common/providers/bridgers/constants/bridgers-native-address");
const to_bridgers_blockchain_1 = require("../../../../../common/providers/bridgers/constants/to-bridgers-blockchain");
const get_from_without_fee_1 = require("../../../../../common/utils/get-from-without-fee");
const token_native_address_proxy_1 = require("../../../../../common/utils/token-native-address-proxy");
const cross_chain_trade_type_1 = require("../../../models/cross-chain-trade-type");
const bridge_type_1 = require("../../common/models/bridge-type");
const ton_cross_chain_trade_1 = require("../../common/ton-cross-chain-trade/ton-cross-chain-trade");
class TonBridgersCrossChainTrade extends ton_cross_chain_trade_1.TonCrossChainTrade {
    get fromContractAddress() {
        throw new Error('Not implemented');
    }
    get methodName() {
        throw new Error('Not implemented');
    }
    constructor(params) {
        super(params.providerAddress, params.routePath, params.useProxy);
        this.type = cross_chain_trade_type_1.CROSS_CHAIN_TRADE_TYPE.BRIDGERS;
        this.isAggregator = false;
        this.onChainSubtype = { from: undefined, to: undefined };
        this.bridgeType = bridge_type_1.BRIDGE_TYPE.BRIDGERS;
        this.from = params.crossChainTrade.from;
        this.to = params.crossChainTrade.to;
        this.priceImpact = this.from.calculatePriceImpactPercent(this.to);
        this.toTokenAmountMin = params.crossChainTrade.toTokenAmountMin;
        this.feeInfo = params.crossChainTrade.feeInfo;
        this.slippage = params.crossChainTrade.slippage;
    }
    async swap(options = {}) {
        if (!options?.testMode) {
            await this.checkTradeErrors();
        }
        await this.checkReceiverAddress(options.receiverAddress, true);
        const config = await this.setTransactionConfig(false, options?.useCacheData || false, options?.receiverAddress || this.walletAddress);
        const { onConfirm } = options;
        let transactionHash;
        const onTransactionHash = (hash) => {
            if (onConfirm) {
                onConfirm(hash);
            }
            transactionHash = hash;
        };
        try {
            await this.web3Private.sendTransaction({ messages: [config], onTransactionHash });
            return transactionHash;
        }
        catch (err) {
            if (err instanceof errors_1.FailedToCheckForTransactionReceiptError) {
                return transactionHash;
            }
            throw err;
        }
    }
    getContractParams() {
        throw new Error('Not implemeted');
    }
    getTradeInfo() {
        return {
            estimatedGas: null,
            feeInfo: this.feeInfo,
            priceImpact: this.priceImpact ?? null,
            slippage: this.slippage * 100,
            routePath: this.routePath
        };
    }
    async getTransactionConfigAndAmount(receiverAddress) {
        const fromBlockchain = this.from.blockchain;
        const toBlockchain = this.to.blockchain;
        const fromWithoutFee = (0, get_from_without_fee_1.getFromWithoutFee)(this.from, this.feeInfo.rubicProxy?.platformFee?.percent);
        const fromTokenAddress = (0, token_native_address_proxy_1.createTokenNativeAddressProxy)(fromWithoutFee, bridgers_native_address_1.bridgersNativeAddress, false).address;
        const toTokenAddress = (0, token_native_address_proxy_1.createTokenNativeAddressProxy)(this.to, bridgers_native_address_1.bridgersNativeAddress, this.to.blockchain !== blockchain_name_1.BLOCKCHAIN_NAME.TRON).address;
        const quoteRequest = {
            fromTokenAddress,
            toTokenAddress,
            fromTokenAmount: fromWithoutFee.stringWeiAmount,
            fromTokenChain: to_bridgers_blockchain_1.toBridgersBlockchain[fromBlockchain],
            toTokenChain: to_bridgers_blockchain_1.toBridgersBlockchain[toBlockchain],
            sourceFlag: 'rubic'
        };
        const quoteResponse = await injector_1.Injector.httpClient.post('https://sswap.swft.pro/api/sswap/quote', quoteRequest);
        const amount = quoteResponse.data?.txData?.amountOutMin;
        const fromAddress = this.walletAddress;
        const swapRequest = {
            fromTokenAddress,
            toTokenAddress,
            fromAddress,
            toAddress: receiverAddress,
            fromTokenChain: to_bridgers_blockchain_1.toBridgersBlockchain[fromBlockchain],
            toTokenChain: to_bridgers_blockchain_1.toBridgersBlockchain[toBlockchain],
            fromTokenAmount: fromWithoutFee.stringWeiAmount,
            amountOutMin: amount,
            equipmentNo: fromAddress.slice(0, 32),
            sourceFlag: 'rubic',
            slippage: this.slippage.toString()
        };
        const swapData = await injector_1.Injector.httpClient.post('https://sswap.swft.pro/api/sswap/swap', swapRequest);
        if (swapData.resCode === 1146)
            throw new not_supported_region_1.NotSupportedRegionError();
        if (!swapData.data?.txData)
            throw new errors_1.NotSupportedTokensError();
        return {
            config: {
                address: swapData.data?.txData.address,
                amount: swapData.data?.txData.amount,
                payload: swapData.data?.txData.payload
            },
            amount
        };
    }
}
exports.TonBridgersCrossChainTrade = TonBridgersCrossChainTrade;
//# sourceMappingURL=ton-bridgers-cross-chain-trade.js.map