import { Estimate } from '../models/lifi-fee-cost';
import { RoutesRequest, RoutesResponse } from '../models/lifi-route';
import { LifiTransactionRequest } from '../models/lifi-transaction-request';
export declare class LifiApiService {
    private static LIFI_API_ENDPOINT;
    static getQuote(fromChain: number, toChain: number, fromToken: string, toToken: string, fromAmount: string, fromAddress: string, toAddress: string, slippage: number, rubicFee?: number): Promise<{
        transactionRequest: LifiTransactionRequest;
        estimate: Estimate;
    }>;
    static getRoutes(request: RoutesRequest): Promise<RoutesResponse>;
}
