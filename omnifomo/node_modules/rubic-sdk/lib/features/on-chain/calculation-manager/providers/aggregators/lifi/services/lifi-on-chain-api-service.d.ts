import { Route, RoutesRequest, RoutesResponse } from "../../../../../../cross-chain/calculation-manager/providers/lifi-provider/models/lifi-route";
import { LifiOnChainTransactionRequest } from '../models/lifi-on-chain-transaction-request';
export declare class LifiOnChainApiService {
    private static LIFI_API_ENDPOINT;
    static getQuote(fromChain: number, toChain: number, fromToken: string, toToken: string, fromAmount: string, fromAddress: string, slippage: number, rubicFee?: number): Promise<{
        transactionRequest: LifiOnChainTransactionRequest;
        estimate: Route;
    }>;
    static getRoutes(request: RoutesRequest): Promise<RoutesResponse>;
}
