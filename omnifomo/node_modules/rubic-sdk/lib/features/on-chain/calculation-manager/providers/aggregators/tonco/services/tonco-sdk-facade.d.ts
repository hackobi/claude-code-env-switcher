import BigNumber from 'bignumber.js';
import { PriceToken, PriceTokenAmount } from "../../../../../../../common/tokens";
import { TonEncodedConfig } from "../../../../../../../core/blockchain/web3-private-service/web3-private/ton-web3-private/models/ton-types";
import { ToncoCommonParams } from '../models/tonco-facade-types';
export declare class ToncoSdkFacade {
    static calculateAmountOut(params: ToncoCommonParams, srcToken: PriceTokenAmount): Promise<BigNumber>;
    static createTonConfig(params: ToncoCommonParams, fromAmountWei: BigNumber, toMinAmountWei: BigNumber): Promise<TonEncodedConfig>;
    /**
     * @returns gasLimit non wei
     */
    static estimateGas(params: ToncoCommonParams, fromAmountWei: BigNumber, toMinAmountWei: BigNumber): Promise<BigNumber>;
    static fetchCommonParams(srcToken: PriceTokenAmount, dstToken: PriceToken): Promise<ToncoCommonParams>;
    private static getSwapType;
}
