import { PriceToken, PriceTokenAmount } from "../../../../../../../common/tokens";
import { StonfiQuoteInfo } from '../models/stonfi-api-types';
export declare class StonfiApiService {
    private static readonly apiUrl;
    static makeQuoteRequest(from: PriceTokenAmount, to: PriceToken, slippage: number): Promise<StonfiQuoteInfo>;
}
