import { PriceTokenAmount, Token } from "../../../../../../../common/tokens";
import { DedustTxStep } from '../models/dedust-api-types';
export declare class DedustApiService {
    private static readonly apiUrl;
    static findBestPools(from: PriceTokenAmount, to: Token): Promise<DedustTxStep[]>;
    private static convertApiTokenAddressToPlain;
}
