import { SolanaToken } from '../models/solana-token';
export declare class SolanaTokensApiService {
    private static readonly apiEndpoint;
    private static readonly newTokensEndpoint;
    private static readonly xApiKey;
    static getTokensList(tokenAddresses: string[]): Promise<SolanaToken[]>;
    static getTokensListOld(tokenAddresses: string[]): Promise<{
        content: SolanaToken[];
    }>;
}
