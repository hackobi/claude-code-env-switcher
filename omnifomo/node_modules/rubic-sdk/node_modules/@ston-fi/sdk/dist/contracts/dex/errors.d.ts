import { DEX_VERSION } from './constants.js';

declare class UnmatchedPtonVersion extends Error {
    constructor({ expected, received, }: {
        expected: DEX_VERSION;
        received: DEX_VERSION;
    });
}

export { UnmatchedPtonVersion };
